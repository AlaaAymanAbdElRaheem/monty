#include "moty.h"

/**
 * pall - prints all the elements of the stack.
 * @top: pointer to thehead node
 */

void pall(stack_t *top)
{
  stack_t *temp = top;
  
	while (temp)
	{
		printf("%d\n", temp->n);
		temp = temp->next;
	}
}
