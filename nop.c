#include "monty.h"

/**
 * nop - doesn’t do anything.
 * @stack: unused argument
 * @line_number: unused argument
 */

void nop(stack_t **stack, unsigned int line_number)
{
	(void) stack;
	(void) line_number;
}
